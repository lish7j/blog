<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lssj.blog.dao.AuthorityMapper">
    <resultMap id="Authority" type="com.lssj.blog.domain.Authority">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
    </resultMap>


    <select id="findById" parameterType="java.lang.Long" resultMap="Authority">
        select
        id, name
        from authority
        where id = #{id}
    </select>
</mapper>