<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tshaojl.blog.dao.BlogMapperr">
    <resultMap id="Blog" type="com.tshaojl.blog.domain.Blog">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="comment_size" jdbcType="INTEGER" property="commentSize"/>
        <result column=""
    </resultMap>


    <select id="findById" parameterType="java.lang.Long" resultMap="Authority">
        select
        id, name
        from authority
        where id = #{id}
    </select>
</mapper>